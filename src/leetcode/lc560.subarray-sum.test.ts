/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
var subarraySum = function(nums: number[], k: number): number {}

test.skip('selection sort', () => {
  expect(subarraySum([1, 1, 1], 2)).toEqual(2)
  expect(subarraySum([1, 2, 1, 2, 1], 3)).toEqual(4)
})
