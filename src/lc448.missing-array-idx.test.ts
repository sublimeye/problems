// https://leetcode.com/problems/find-all-numbers-disappeared-in-an-array/
var findDisappearedNumbers = function(nums) {
  // iterate over an array
  // start moving "in place" elements to correct position
  // once done -> iterate and see what's missing
  // save a pointer to proceed ...
  // could just modify it to "true" and see what's false
  // use _negative_number_
  // take absolute ->
  // use zeroes as markers
}

test.skip('push dominoes', () => {
  expect(findDisappearedNumbers([0, 1])).toEqual([1])
})
